version: '3.8'

services:
  bot:
    build: .
    container_name: social_chat_bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./bot_data.db:/app/bot_data.db
      - ./config.json:/app/config.json:ro
    command: python bot.py
